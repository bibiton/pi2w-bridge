# Binaries
pi2w-bridge
pi2w-bridge-arm
pi2w-bridge-arm7
pi2w-helper-arm

# Environment
.env

# MQTT config (contains credentials)
mqtt_config.json

# Robot config (contains IPs / secrets)
robots.yaml

# SQLite database (created in the working directory by default)
*.db
*.db-shm
*.db-wal
